# Static geospatial data

Place the validated WGS84 Bucharest sector boundary file here as:

`bucharest-sectors.geojson`

Do not add provisional or hand-drawn polygons. The required schema is documented in `docs/FRONTEND_DATA_CONTRACT.md`. The dashboard works with a basemap-only state while this file is absent.
